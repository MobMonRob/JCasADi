package de.dhbw.rahmlab.casadi.api.core.interfaces;

/**
 * Marker interface for symbolic expressions within the framework.
 * This interface is used to indicate that a class represents a symbolic expression.
 * It does not define any methods, serving solely as a type identifier.
 *
 * Note: The commented methods suggest potential functionality for setting bounds
 * and evaluating expressions, but are not implemented in this marker interface.
 */
public interface SymbolicExpression {

    // void setBounds(double lowerBound, double upperBound);

    // DMWrapper evaluate(Map<String, DMWrapper> variables);

}
